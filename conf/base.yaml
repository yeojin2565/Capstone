---

num_rounds: 200
num_clients: 36
batch_size: 32
num_classes: 10
num_clients_per_round_fit: 4  # 선택되는 클라이언트 수
num_clients_per_round_eval: 10 
config_fit:
  lr: 0.01
  momentum: 0.9
  local_epochs: 2