---

num_rounds: 100
num_clients: 100                  # 전체 클라이언트
batch_size: 20                   
num_classes: 10
num_clients_per_round_fit: 10    # 매 라운드 당 학습에 참여하는 클라이언트 수 
num_clients_per_round_eval: 25   # 매 라운드 당 평가에 참여하는 클라이언트 수
config_fit:
  lr: 0.01
  momentum: 0.9
  local_epochs: 1